import { RadioTabsDirective } from './radio-tabs.directive';

describe('RadioTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioTabsDirective(null);
    expect(directive).toBeTruthy();
  });
});
